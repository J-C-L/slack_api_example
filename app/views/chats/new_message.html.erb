<!--  Can't use form_for since there's not active record model/database -->

<%= form_tag send_message_path(@channel.name), method: :post do %>
  <%= label_tag :messge %>
  <%= text_field_tag :message %>

  <%= submit_tag "Send it!" %>
<% end %>
